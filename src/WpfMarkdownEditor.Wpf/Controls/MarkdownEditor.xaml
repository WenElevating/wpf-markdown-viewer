<UserControl x:Class="WpfMarkdownEditor.Wpf.Controls.MarkdownEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:WpfMarkdownEditor.Wpf.Converters">
    <UserControl.Resources>
        <c:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <!-- Context Menu Brushes -->
        <SolidColorBrush x:Key="MenuBackground" Color="#ffffff"/>
        <SolidColorBrush x:Key="MenuBorder" Color="#e5e5e5"/>
        <SolidColorBrush x:Key="MenuForeground" Color="#1a1a1a"/>
        <SolidColorBrush x:Key="MenuHoverBackground" Color="#f5f5f5"/>
        <SolidColorBrush x:Key="MenuSeparator" Color="#e5e5e5"/>
        <SolidColorBrush x:Key="MenuDisabledForeground" Color="#b0b0b0"/>

        <!-- Context Menu -->
        <Style x:Key="EditorContextMenuStyle" TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource MenuBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource MenuBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="HasDropShadow" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <StackPanel IsItemsHost="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Menu Item -->
        <Style x:Key="EditorMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Foreground" Value="{DynamicResource MenuForeground}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" Content="{TemplateBinding Icon}"
                                                  VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <ContentPresenter Grid.Column="1" ContentSource="Header"
                                                  VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="{TemplateBinding InputGestureText}"
                                           FontSize="11" Opacity="0.5" VerticalAlignment="Center"
                                           Margin="20,0,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource MenuHoverBackground}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource MenuDisabledForeground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Menu Separator -->
        <Style x:Key="EditorMenuSeparatorStyle" TargetType="Separator">
            <Setter Property="Background" Value="{DynamicResource MenuSeparator}"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="4,4"/>
        </Style>

        <!-- Theme-aware toolbar brushes (updated in code-behind) -->
        <SolidColorBrush x:Key="ToolbarBackground" Color="#f6f8fa"/>
        <SolidColorBrush x:Key="ToolbarBorder" Color="#d0d7de"/>
        <SolidColorBrush x:Key="ToolbarForeground" Color="#24292f"/>
        <SolidColorBrush x:Key="ToolbarHoverBackground" Color="#18000000"/>
        <SolidColorBrush x:Key="ToolbarPressedBackground" Color="#28000000"/>
        <SolidColorBrush x:Key="ToolbarAccent" Color="#0969da"/>

        <!-- Flat toolbar button -->
        <Style x:Key="ToolBarFlatButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ToolbarForeground}"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ToolbarHoverBackground}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ToolbarPressedBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Minimal slider for zoom -->
        <Style x:Key="ToolBarFlatSlider" TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <!-- Track background -->
                            <Border Height="3" VerticalAlignment="Center"
                                    CornerRadius="1.5" Background="{DynamicResource ToolbarBorder}" Opacity="0.6"/>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate><Border Background="Transparent"/></ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate><Border Background="Transparent"/></ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate>
                                                <Ellipse Width="12" Height="12"
                                                         Fill="{DynamicResource ToolbarAccent}"
                                                         Opacity="0.9"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="{Binding PreviewWidth, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </Grid.ColumnDefinitions>

        <!-- Editor Pane -->
        <TextBox x:Name="EditorTextBox"
                 Grid.Column="0"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 PreviewKeyDown="OnEditorPreviewKeyDown"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Disabled"
                 TextWrapping="Wrap"
                 Text="{Binding Markdown, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 TextChanged="OnEditorTextChanged"
                 FontFamily="Cascadia Code, Consolas, Liberation Mono"
                 FontSize="14"
                 BorderThickness="0"
                 Padding="12,8">
	            <TextBox.ContextMenu>
	                <ContextMenu x:Name="EditorContextMenu"
	                             Style="{StaticResource EditorContextMenuStyle}">
	                    <MenuItem Command="ApplicationCommands.Undo" Style="{StaticResource EditorMenuItemStyle}" Header="Undo">
	                        <MenuItem.Icon><TextBlock Text="&#xE7A7;" FontFamily="Segoe MDL2 Assets" FontSize="14"/></MenuItem.Icon>
	                    </MenuItem>
	                    <MenuItem Command="ApplicationCommands.Redo" Style="{StaticResource EditorMenuItemStyle}" Header="Redo">
	                        <MenuItem.Icon><TextBlock Text="&#xE7A6;" FontFamily="Segoe MDL2 Assets" FontSize="14"/></MenuItem.Icon>
	                    </MenuItem>
	                    <Separator Style="{StaticResource EditorMenuSeparatorStyle}"/>
	                    <MenuItem Command="ApplicationCommands.Cut" Style="{StaticResource EditorMenuItemStyle}" Header="Cut">
	                        <MenuItem.Icon><TextBlock Text="&#xE8C6;" FontFamily="Segoe MDL2 Assets" FontSize="14"/></MenuItem.Icon>
	                    </MenuItem>
	                    <MenuItem Command="ApplicationCommands.Copy" Style="{StaticResource EditorMenuItemStyle}" Header="Copy">
	                        <MenuItem.Icon><TextBlock Text="&#xE8C8;" FontFamily="Segoe MDL2 Assets" FontSize="14"/></MenuItem.Icon>
	                    </MenuItem>
	                    <MenuItem Command="ApplicationCommands.Paste" Style="{StaticResource EditorMenuItemStyle}" Header="Paste">
	                        <MenuItem.Icon><TextBlock Text="&#xE77F;" FontFamily="Segoe MDL2 Assets" FontSize="14"/></MenuItem.Icon>
	                    </MenuItem>
	                    <Separator Style="{StaticResource EditorMenuSeparatorStyle}"/>
	                    <MenuItem Command="ApplicationCommands.SelectAll" Style="{StaticResource EditorMenuItemStyle}" Header="Select All"/>
	                </ContextMenu>
	            </TextBox.ContextMenu>
	        </TextBox>

        <!-- Splitter -->
        <GridSplitter x:Name="EditorSplitter" Grid.Column="1"
                      Width="6"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Stretch"
                      Background="Transparent" />

        <!-- Preview Pane with modern toolbar -->
        <Grid Grid.Column="2"
              Visibility="{Binding ShowPreview, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Document Viewer -->
            <FlowDocumentScrollViewer x:Name="PreviewViewer"
                                      Grid.Row="0"
                                      IsToolBarVisible="False"
                                      VerticalScrollBarVisibility="Auto">
                <FlowDocumentScrollViewer.Resources>
                    <!-- Minimal scrollbar -->
                    <Style TargetType="ScrollBar">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Width" Value="8"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ScrollBar">
                                    <Grid Background="Transparent">
                                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                                            <Track.Thumb>
                                                <Thumb x:Name="Thumb" Opacity="0.3">
                                                    <Thumb.Template>
                                                        <ControlTemplate TargetType="{x:Type Thumb}">
                                                            <Border x:Name="Bd" Background="#888" CornerRadius="4"/>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsDragging" Value="True">
                                                                    <Setter TargetName="Bd" Property="Opacity" Value="0.9"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Thumb.Template>
                                                </Thumb>
                                            </Track.Thumb>
                                        </Track>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Thumb" Property="Opacity" Value="0.6"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </FlowDocumentScrollViewer.Resources>
            </FlowDocumentScrollViewer>

            <!-- Modern Preview Toolbar -->
            <Border x:Name="PreviewToolbarBorder"
                    Grid.Row="1"
                    BorderThickness="0,1,0,0"
                    Padding="8,4,8,4">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">

                    <!-- Zoom Out -->
                    <Button x:Name="ZoomOutBtn" Click="OnZoomOut"
                            ToolTip="Zoom Out"
                            Style="{StaticResource ToolBarFlatButton}">
                        <TextBlock Text="&#xE949;" FontFamily="Segoe MDL2 Assets" FontSize="11" Margin="0,-1,0,0"/>
                    </Button>

                    <!-- Zoom Slider -->
                    <Slider x:Name="ZoomSlider"
                            Width="100" Height="20"
                            Minimum="50" Maximum="200"
                            Value="100"
                            TickFrequency="10"
                            IsSnapToTickEnabled="True"
                            Margin="8,0"
                            ValueChanged="OnZoomSliderChanged"
                            Style="{StaticResource ToolBarFlatSlider}"/>

                    <!-- Zoom Percentage -->
                    <TextBlock x:Name="ZoomText"
                               Text="100%"
                               FontFamily="Segoe UI Variable, Segoe UI"
                               FontSize="11"
                               VerticalAlignment="Center"
                               Width="36"
                               TextAlignment="Center"/>

                    <!-- Zoom In -->
                    <Button x:Name="ZoomInBtn" Click="OnZoomIn"
                            ToolTip="Zoom In"
                            Style="{StaticResource ToolBarFlatButton}">
                        <TextBlock Text="&#xE948;" FontFamily="Segoe MDL2 Assets" FontSize="11" Margin="0,-1,0,0"/>
                    </Button>

                    <!-- Separator -->
                    <Border Width="1" Height="14" Background="#d0d7de" Margin="10,0"
                            x:Name="ToolBarSeparator"/>

                    <!-- Reset Zoom -->
                    <Button x:Name="ZoomResetBtn" Click="OnZoomReset"
                            ToolTip="Reset Zoom"
                            Style="{StaticResource ToolBarFlatButton}">
                        <TextBlock Text="&#xE777;" FontFamily="Segoe MDL2 Assets" FontSize="11" Margin="0,-1,0,0"/>
                    </Button>

                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>